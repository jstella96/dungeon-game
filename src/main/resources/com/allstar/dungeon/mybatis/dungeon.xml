<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace속성:매퍼파일의 완전한경로 .xml는 생략 -->

<mapper namespace="com.allstar.dungeon.mybatis.dungeon">
	
	
	<select id="dungeonDiamondSelect" parameterType="java.util.Map" resultType="java.util.Map">
		select d.no as no, map, x, y, state from diamondget g join diamond d 
		on g.no = d.no where id ='${id}' and state = 'true' and d.map=${page}
	</select>
	<update id="dungeonDiamondUpdate" parameterType="java.util.Map">
		UPDATE diamondget SET state='false' WHERE no = ${diamondNumber} and id='${id}'
	</update>
    <update id="CharacterLocationUpdate" parameterType="java.util.Map">
		UPDATE member SET map=${page}, x=${x}, y=${y} WHERE id='${id}'
	</update>
	<insert id="testInsert">
		insert into dungeontest values('ddd')
	</insert>
	
	
</mapper>